<h1>商品詳細</h1>
<%= attachment_image_tag @item, :image, :fill, 225, 225 %>
<table>
 <tbody>
  <tr>
   <td>商品名</td>
   <td><%= @item.name %></td>
  </tr>
  <tr>
   <td>商品説明</td>
   <td><%= @item.introduction %></td>
  </tr>
  <tr>
   <td>ジャンル</td>
   <td><%= @item.genre.name %></td>
  </tr>
  <tr>
   <td>税込価格（税抜き価格）</td>
   <td><%= (@item.price*1.1).round(0) %>円(<%= @item.price %>円)</td>
  </tr>
  <tr>
   <td>販売ステータス</td>
   <td> <% if @item.is_active == true %>
         販売中
        <% else %>
         販売停止中
        <% end %>
    </td>
  </tr>
 </tbody>
</table>
<%= link_to  "編集する", edit_admin_item_path %>
<%= link_to "削除する", admin_item_path(@item.id), method: :delete %>